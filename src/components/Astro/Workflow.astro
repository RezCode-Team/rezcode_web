---
import { Search, PenTool, Code2, Rocket } from "lucide-react";
---

<section class="py-24 bg-brand-dark overflow-hidden relative" id="workflow">
  <div class="absolute inset-0 bg-brand-primary/5 opacity-50 tech-grid"></div>

  <div class="max-w-7xl mx-auto px-6 relative z-10">
    <div class="text-center mb-20" data-aos="fade-up">
      <h2 class="text-brand-secondary font-bold tracking-widest uppercase mb-4">
        The Algorithm
      </h2>
      <h3 class="text-4xl md:text-5xl font-black text-white">How We Operate</h3>
    </div>

    <div class="relative grid md:grid-cols-4 gap-8">
      {/* Connecting Line (Desktop) */}
      <div
        class="hidden md:block absolute top-12 left-0 w-full h-0.5 bg-gradient-to-r from-transparent via-brand-primary/30 to-transparent"
      >
      </div>

      {/* Step 1 */}
      <div data-aos="fade-up" data-aos-delay="0" class="relative group">
        <div
          class="w-24 h-24 mx-auto bg-brand-dark border-2 border-brand-primary/30 rounded-full flex items-center justify-center mb-6 relative z-10 group-hover:border-brand-primary group-hover:scale-110 transition-all shadow-[0_0_20px_rgba(80,122,189,0.2)]"
        >
          <Search className="w-10 h-10 text-brand-primary" />
        </div>
        <div class="text-center">
          <h4 class="text-xl font-bold text-white mb-2">Analyze</h4>
          <p class="text-sm text-brand-secondary leading-relaxed">
            Dissecting the problem space to find the optimal trajectory.
          </p>
        </div>
      </div>

      {/* Step 2 */}
      <div data-aos="fade-up" data-aos-delay="100" class="relative group">
        <div
          class="w-24 h-24 mx-auto bg-brand-dark border-2 border-brand-primary/30 rounded-full flex items-center justify-center mb-6 relative z-10 group-hover:border-brand-primary group-hover:scale-110 transition-all shadow-[0_0_20px_rgba(80,122,189,0.2)]"
        >
          <PenTool className="w-10 h-10 text-brand-primary" />
        </div>
        <div class="text-center">
          <h4 class="text-xl font-bold text-white mb-2">Architect</h4>
          <p class="text-sm text-brand-secondary leading-relaxed">
            Designing robust systems combining aesthetics and scalability.
          </p>
        </div>
      </div>

      {/* Step 3 */}
      <div data-aos="fade-up" data-aos-delay="200" class="relative group">
        <div
          class="w-24 h-24 mx-auto bg-brand-dark border-2 border-brand-primary/30 rounded-full flex items-center justify-center mb-6 relative z-10 group-hover:border-brand-primary group-hover:scale-110 transition-all shadow-[0_0_20px_rgba(80,122,189,0.2)]"
        >
          <Code2 className="w-10 h-10 text-brand-primary" />
        </div>
        <div class="text-center">
          <h4 class="text-xl font-bold text-white mb-2">Develop</h4>
          <p class="text-sm text-brand-secondary leading-relaxed">
            Writing clean, efficient, and maintainable code.
          </p>
        </div>
      </div>

      {/* Step 4 */}
      <div data-aos="fade-up" data-aos-delay="300" class="relative group">
        <div
          class="w-24 h-24 mx-auto bg-brand-dark border-2 border-brand-primary/30 rounded-full flex items-center justify-center mb-6 relative z-10 group-hover:border-brand-primary group-hover:scale-110 transition-all shadow-[0_0_20px_rgba(80,122,189,0.2)]"
        >
          <Rocket className="w-10 h-10 text-brand-primary" />
        </div>
        <div class="text-center">
          <h4 class="text-xl font-bold text-white mb-2">Launch</h4>
          <p class="text-sm text-brand-secondary leading-relaxed">
            Deploying to global edge networks for instant availability.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>
